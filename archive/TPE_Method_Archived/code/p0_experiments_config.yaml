# P0 Experiments Pipeline Configuration

# Dataset paths
dataset_path: /home/kyrie/毕设/data_generation/output/level2_tpe/cornell-box_test
dataset_2k_path: /home/kyrie/毕设/data_generation/output/dataset_2k
scene_path: /home/kyrie/毕设/data_generation/scenes/cornell-box/scene.xml

# Output directory
output_dir: /home/kyrie/毕设/multi_time_compression/output/p0_results

# Checkpoint file
checkpoint_file: p0_checkpoint.json

# Experiment F: Ground Truth Validation
experiment_f:
  selected_hours: [6, 9, 15, 18]
  spp: 256
  num_samples: 64

# Experiment 2: Residual Analysis
experiment_2:
  residual_threshold_percent: 5.0

# Experiment 3: Spatial Generalization
experiment_3:
  num_probes: 10
  max_workers: 4
  direction_mode: pca

# Experiment 4: Baseline Comparison
experiment_4:
  baselines: ['spline', 'direct_mlp', 'tdml']
  cross_validation: 'odd_even'
